line-length = 100

[lint]
select = ["E", "F", "I", "B", "PIE", "RUF"]

# The desktop schedule intentionally uses local wall-clock timestamps and
# Chinese punctuation in user-facing strings.
ignore = ["DTZ001", "DTZ005", "RUF001"]

[lint.per-file-ignores]
"tests/*.py" = ["E402"]
